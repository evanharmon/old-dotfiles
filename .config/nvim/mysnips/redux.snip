snippet     reducer
abbr        const reducer = () => {}
options     head
  const ${1:reducer} = (state = ${2}, action) => {
    switch (action.type) {
      ${3}
      default:
        return state;
    }
  };
