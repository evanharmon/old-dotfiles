name: Docker Image CI

on: [push]

jobs:

  build:
 
    runs-on: ubuntu-latest
 
    steps:
    - uses: actions/checkout@v1
    - name: Build the Base Docker image
      run: cat .config/docker/Dockerfile.amazonlinux.devtools | docker build my-image-name:$(date +%s)
